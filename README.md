# birdr

Monorepo for the birdr project.

## Structure

- `app/` — Mobile app code (frontend / client).
- `functions/` — Backend logic, implemented as Supabase functions.

## Getting started

Tooling for each subproject lives inside its own folder. See `app/README.md` and `functions/README.md` (to be added) for setup instructions specific to each.
