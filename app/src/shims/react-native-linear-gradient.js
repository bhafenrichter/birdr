// Shim: react-native-linear-gradient → expo-linear-gradient
// The skeleton library expects a default export, but expo uses a named export.
import { LinearGradient } from "expo-linear-gradient";
export default LinearGradient;
